services:
  # Frontend only (backend already deployed separately)
  - type: web
    name: larp-detector-frontend
    env: docker
    dockerfilePath: ./frontend/Dockerfile
    plan: free
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://larp-detector.onrender.com
